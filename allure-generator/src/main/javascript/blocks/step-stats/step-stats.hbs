<span class="step-stats__info">
    <span>
    {{~#if hasContent}}
    (
         {{~#if parameters.length ~}}
            {{~t 'testCase.stats.count.parameters' count=parameters.length~}}
             <span class="step-stats__info_separator">,&nbsp;</span>
         {{~/if~}}
         {{~#if stepsCount ~}}
            {{~t 'testCase.stats.count.steps' count=stepsCount~}}
            <span class="step-stats__info_separator">,&nbsp;</span>
        {{~/if~}}
        {{~#if attachmentsCount ~}}
            {{~t 'testCase.stats.count.attachments' count=attachmentsCount~}}
            <span class="step-stats__info_separator">,&nbsp;</span>
        {{~/if~}}
    )
    {{~/if~}}
    </span>
    <span class="step-stats__info_time" data-tooltip="{{date time.start}} {{time time.start true}}&nbsp;&ndash;&nbsp;{{time time.stop true}}">{{duration time.duration}}</span>
</span>
