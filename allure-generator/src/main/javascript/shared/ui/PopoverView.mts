import "./styles/PopoverView.scss";
import TooltipView from "./TooltipView.mts";

class PopoverView extends TooltipView {
  className = "popover";

  positionClassBase = "popover";
}

export default PopoverView;
